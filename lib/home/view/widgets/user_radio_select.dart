import 'package:flutter/material.dart';

class UserRadioSelect extends StatelessWidget {
  const UserRadioSelect({super.key});

  @override
  Widget build(BuildContext context) {
    return Wrap(
      children: [
        Container(),
        Container(),
        SizedBox(),
      ],
    );
  }
}
